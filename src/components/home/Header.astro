<header>
  <div class="wrapper">
    <div class="text">
      <h1>La firma electrónica más fácil de usar</h1>
      <h2>
        Simplifica los procesos de firma de tu empresa. Crea, envía,
        firma y almacena documentos de forma segura con validez legal.
      </h2>
      <div class="actions">
        <button class="gradient transition-up">Contactar Ventas</button>
        <button class="outline transition-up">
          <i>
            <svg width="32" height="32" viewBox="0 0 24 24"
              ><path
                fill="currentColor"
                d="M9 17.192V6.808L17.154 12z"></path></svg
            >
          </i>
          Ver video
        </button>
      </div>
    </div>
    <div class="image">
      <img src="/public/images/screen.png" alt="header firmeasy" />
    </div>
  </div>
</header>
<style>
  header {
    .wrapper {
      display: flex;
      align-items: center;
      justify-content: space-between;
      max-width: var(--container-max-width);
      width: 100%;
      height: 100%;
      min-height: 560px;
      padding: 0 var(--container-padding);
      box-sizing: border-box;
      margin: 0 auto;
      .image {
        img {
          width: 100%;
          max-width: 560px;
        }
      }
      .text {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        color: var(--text-color);
        h1 {
          font-size: var(--title-size-xl);
          font-weight: 600;
          text-wrap: balance;
          max-width: 500px;
          line-height: var(--title-line-height-xl);
        }
        h2 {
          font-size: var(--title-size);
          line-height: var(--title-line-height);
          text-wrap: pretty;
          max-width: 520px;
          font-weight: 300;
        }
        .actions {
          display: flex;
          align-items: center;
          gap: 1rem;
          margin-top: 1rem;
        }
      }
      .image {
        border: 1px solid var(--border-color);
        border-radius: var(--border-radius);
        overflow: hidden;
        box-shadow: var(--shadow);
        img {
          display: block;
        }
      }
    }
  }
</style>
