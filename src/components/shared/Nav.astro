---
import { nav } from '@site';
import IconMenu from '../icons/IconMenu.astro';
import IconClose from '../icons/IconClose.astro';
---

<nav>
  <div class="wrapper">
    <div class="logo">
      <a href="/">
        <img src="/images/logo.png" alt="logo firmeasy" />
      </a>
      <a href="/verificacion" class="verified">
        <img src="/images/verifiedSunat.svg" alt="verificado firmeasy" />
      </a>
    </div>
    <input type="checkbox" id="menu" class="menu-button" />
    <div class="menu">
      <ul>
        {
          nav.menu.map((item) => (
            <li>
              <a href={item.link}>{item.name}</a>
            </li>
          ))
        }
      </ul>
      <div class="actions">
        <a
          href={nav.button3.link}
          class="button outline transition-up">{nav.button3.text}</a
        >
        
        <a
          href={nav.button1.link}
          class="button outline transition-up">{nav.button1.text}</a
        >
        
        <a
          href={nav.button2.link}
          class="button gradient transition-up">{nav.button2.text}</a
        >
      </div>
      <label for="menu" class="menu-button">
        <i>
          <IconMenu />
        </i>
        <i>
          <IconClose />
        </i>
      </label>
    </div>
  </div>
</nav>
<style lang="scss">
  @import '../../assets/breakpoints.scss';
  nav {
    height: var(--nav-height);
    width: 100%;
    border-bottom: 1px solid var(--border-color);
    position: sticky;
    top: 0;
    background-color: white;
    z-index: 5;
    .wrapper {
      max-width: var(--container-max-width);
      width: 100%;
      height: 100%;
      padding: 0 var(--container-padding);
      box-sizing: border-box;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      .logo {
        display: flex;
        align-items: center;
        gap: .25rem;
        & > a > img {
          height: 36px;
          @include screen('sm') {
            height: 28px;
          }
        }
        .verified {
          padding-top: .3rem;
          img {
            height: 6rem;
            @include screen('sm') {
              height: 5rem;
            }
          }
        }
      }

      .menu {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        height: 100%;
        font-size: var(--text-size);
        font-weight: 500;
        @include screen('sm') {
          gap: 1rem;
        }
        ul {
          list-style: none;
          display: flex;
          align-items: center;
          height: 100%;
          gap: 1.5rem;
          @include screen('sm') {
            gap: 1rem;
          }
          @include screen('lg') {
            position: absolute;
            top: var(--nav-height);
            left: 0;
            width: 100%;
            height: auto;
            z-index: 10;
            background-color: white;
            border: 1px solid var(--border-color);
            padding: var(--container-padding);
            flex-wrap: wrap;
            box-shadow: var(--shadow);
            display: none;
            box-sizing: border-box;
          }
          li {
            a {
              color: var(--text-color);
              text-decoration: none;
            }
          }
        }
        .actions {
          display: flex;
          align-items: center;
          gap: 1rem;
          flex-wrap: nowrap;
          @include screen('sm') {
            & > a:nth-last-child(-n+2) {
              display: none;
            }
          }
          @media screen and (max-width: 375px) {
            & > a:nth-last-child(-n+2) {
              // display: none;
            }
          }
        }
        .menu-button {
          display: none;
          i {
            display: none;
            svg {
              width: 24px;
              height: 24px;
            }
          }
          @include screen('lg') {
            display: block;
            i {
              &:first-child {
                display: block;
              }
              &:last-child {
                display: none;
              }
            }
          }
        }
      }
      #menu {
        display: none;
        @include screen('lg') {
          &:checked ~ .menu {
            ul {
              display: flex;
            }
            .menu-button {
              i {
                &:first-child {
                  display: none;
                }
                &:last-child {
                  display: block;
                }
              }
            }
          }
        }
      }
    }
  }
</style>
